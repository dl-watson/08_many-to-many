const pool = require("../utils/pool");

module.exports = class Student {
  id;
  name;

  constructor(row) {
    this.id = row.id;
    this.name = row.name;
  }

  // INSERT

  // GET

  // GET BY ID

  // UPDATE

  // DELETE
};
