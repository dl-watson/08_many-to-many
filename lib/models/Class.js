const pool = require("../utils/pool");

module.exports = class Class {
  id;
  title;

  constructor(row) {
    this.id = row.id;
    this.title = row.title;
  }

  // INSERT

  // GET

  // GET BY ID

  // UPDATE

  // DELETE
};
